export enum AppointmentStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  IN_PROGRESS = 'in_progress',
  DECLINED = 'declined',
  CANCELED = 'canceled',
  NO_SHOW = 'no_show',
  COMPLETED = 'completed',
  OVERDUE = 'overdue',
}
