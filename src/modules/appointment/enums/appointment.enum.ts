export enum AppointmentStatus {
  AWAITING_PAYMENT = 'awaiting_payment',
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  IN_PROGRESS = 'in_progress',
  UPCOMING = 'upcoming',
  DECLINED = 'declined',
  CANCELED = 'canceled',
  NO_SHOW = 'no_show',
  COMPLETED = 'completed',
  OVERDUE = 'overdue',
}
